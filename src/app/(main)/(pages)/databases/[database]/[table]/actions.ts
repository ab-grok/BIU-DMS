type table = {
  name: string;
  rowCount: number;
  description: string;
  createdAt: string;
  updatedAt: string;
};
// export async function listTables(): Promise<{ items: Array<string>|null}>{
//     const res = await
// }
